import pytest
from mavimage.camera import Camera
from io import BytesIO
from PIL import Image


def test_init():
    #class Capture:
       # Capture.image = Image.new('RGBA', size=(16, 16), color=(155, 0, 0))

    #test = Capture(1.0)
    #assert (test.image == 1.0)
    pass
